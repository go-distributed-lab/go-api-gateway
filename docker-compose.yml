services:
  gateway:
    build:
      context: .
      target: gateway
    profiles: [full]
    ports:
      - "8080:8080"
    depends_on:
      - upstream

  upstream:
    build:
      context: .
      target: upstream
    profiles: [full]
    ports:
      - "9001:9001"